global:
  scrape_interval: 15s
  scrape_timeout: 10s
  evaluation_interval: 15s

scrape_configs:
  - job_name: proxier
    metrics_path: /metrics
    static_configs:
      - targets:
          - proxier:8080
